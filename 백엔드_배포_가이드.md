# 🔧 백엔드 API 별도 배포 가이드

## 📋 상황 설명

Vercel은 프론트엔드 배포에 최적화되어 있어서:
- ✅ **프론트엔드**: Vercel에서 배포
- ✅ **백엔드 API**: Railway 또는 Render에서 별도 배포

---

## 🚀 옵션 1: Railway 배포 (권장)

### 1단계: Railway 계정 생성
- https://railway.app 접속
- "Start a New Project" 클릭

### 2단계: GitHub 연결
- "Deploy from GitHub repo" 선택
- `space_recommendation` 저장소 선택

### 3단계: 설정
```
Root Directory: api
Build Command: (비워두기)
Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
```

### 4단계: 환경 변수
Railway 대시보드에서 설정:
```
PORT=8000
```

### 5단계: 배포 완료
배포 URL 복사:
```
https://your-api.railway.app
```

---

## 🚀 옵션 2: Render 배포

### 1단계: Render 계정 생성
- https://render.com 접속

### 2단계: 새 Web Service 만들기
- "New +" → "Web Service"
- GitHub 저장소 연결

### 3단계: 설정
```
Name: space-recommendation-api
Root Directory: api
Build Command: pip install -r requirements.txt
Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
```

### 4단계: 환경 변수
```
PORT=8000
PYTHON_VERSION=3.9
```

### 5단계: 무료 플랜 선택
- "Free" 플랜 선택
- "Create Web Service" 클릭

---

## 🔗 프론트엔드와 연결

### 1. 백엔드 배포 URL 복사
예: `https://space-recommendation-api.railway.app`

### 2. Vercel 환경 변수 업데이트
1. Vercel 대시보드 → 프로젝트 선택
2. Settings → Environment Variables
3. 추가/수정:
   ```
   Name: NEXT_PUBLIC_API_URL
   Value: https://space-recommendation-api.railway.app
   ```
4. "Add" 클릭

### 3. Vercel 재배포
1. Deployments 탭
2. 최신 배포에서 "..." 클릭
3. "Redeploy" 선택

---

## ✅ 테스트

### 1. 백엔드 확인
```bash
curl https://your-api.railway.app/
```

응답:
```json
{
  "message": "서울 카드 데이터 기반 추천 서비스 API",
  "version": "1.0.0"
}
```

### 2. 프론트엔드 확인
```
https://space-recommendation.vercel.app
```

### 3. 통합 테스트
- 프론트엔드에서 추천 폼 입력
- 결과 확인

---

## 📊 배포 상태

### 현재
- ✅ 프론트엔드: Vercel (배포 진행 중)
- ⏳ 백엔드: 별도 배포 필요

### 다음 단계
1. Railway 또는 Render에서 백엔드 배포
2. Vercel 환경 변수 설정
3. 재배포

---

## 💡 참고사항

### 로컬 개발 시
```bash
# 백엔드
cd api
uvicorn main:app --reload --port 8000

# 프론트엔드
npm run dev
```

### 데이터 파일
백엔드 배포 시 필요한 파일:
- `outputs/step1_static_profiles.json`
- `outputs/realtime_area_profiles.json`

이 파일들을 Railway/Render에 포함시켜야 합니다.

---

**작성일**: 2025-12-10

