# 🎯 STEP 1 + STEP 2 통합 추천 시스템 완벽 가이드

## 📋 시스템 개요

이 시스템은 **서울시 카드 데이터(STEP 1)**와 **실시간 지역 점수(STEP 2)**를 통합하여 
사용자에게 최적의 지역을 추천하는 AI 기반 추천 시스템입니다.

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   INPUT     │     │   STEP 1    │     │   STEP 2    │
│             │     │             │     │             │
│ 사용자 정보  │ ──> │ 정적 집단   │ ──> │ 실시간 지역 │
│ 나이/성별   │     │ 프로필 생성 │     │ 점수 계산   │
│ 소득/취향   │     │             │     │             │
└─────────────┘     └─────────────┘     └─────────────┘
                              │                  │
                              └──────┬───────────┘
                                     ▼
                            ┌─────────────┐
                            │   STEP 3    │
                            │             │
                            │ 사용자-지역 │
                            │   매칭      │
                            │             │
                            └─────────────┘
                                     │
                                     ▼
                            ┌─────────────┐
                            │   OUTPUT    │
                            │             │
                            │ TOP 5 지역  │
                            │ 추천 결과   │
                            └─────────────┘
```

---

## 🚀 빠른 시작

### 1단계: STEP 1 프로필 생성

```bash
# 카드 데이터 기반 정적 프로필 생성
python3 step1_static_profile_generation.py
```

**생성 파일**:
- `outputs/step1_static_profiles.json` - 14개 세그먼트 프로필
- `outputs/step1_segment_matcher.json` - 매칭 테이블
- `outputs/step1_time_patterns.json` - 시간대별 패턴
- `outputs/step1_summary_report.md` - 요약 리포트

### 2단계: STEP 2 지역 점수 준비

STEP 2는 다른 에이전트가 생성합니다. 다음 형식의 파일이 필요합니다:

**필요한 파일 예시**: `outputs/seoul_all_gu_final.csv`

```csv
구,특징,업종다양성,소비안정성,성장률
강남구,학원/학습지 특화 (31.4%),보통(14개),안정적(16.8%),0.62% (→유지)
마포구,ZZ_나머지 특화 (52.7%),낮음(8개),안정적(17.1%),+1.67% (↑상승)
...
```

### 3단계: 통합 추천 시스템 실행

```python
from step3_integrated_recommendation import IntegratedRecommendationSystem

# 시스템 초기화
system = IntegratedRecommendationSystem(
    step1_dir='outputs',
    step2_file='outputs/seoul_all_gu_final.csv'
)

# 사용자 맞춤 추천
result = system.recommend_regions(
    age=28,
    gender='남',
    income_level='중',
    preferences={
        'preferred_industries': ['한식', '커피전문점', '영화/공연'],
        'time_preference': '저녁',
        'budget': 30000,
        'priorities': {
            'preference_match': 0.6,  # 선호도 가중치
            'score': 0.4              # 지역 점수 가중치
        }
    },
    top_n=5,
    use_step2=True
)

# 결과 출력
system.print_recommendations(result)
```

---

## 📊 추천 알고리즘 상세

### STEP 1: 사용자 프로필 매칭

#### 1.1 세그먼트 결정
```python
나이 28세 → 연령대 '20대' (20_29세)
성별 '남' → 성별 'male'
→ 세그먼트 ID: '20s_male'
```

#### 1.2 세그먼트 특성
- **총 소비액**: 78억원
- **평균 거래액**: 9,333원
- **주요 관심사**: 
  1. 기타요식 (26.2%)
  2. 편의점 (23.1%)
  3. 영화/공연 (18.1%)

#### 1.3 소득 수준 보정
```python
income_multipliers = {
    '저': 0.7,   # 평균의 70%
    '중': 1.0,   # 평균 그대로
    '고': 1.5    # 평균의 150%
}

조정된 평균 거래액 = 9,333원 × 1.0 = 9,333원
```

---

### STEP 2: 지역 점수 계산

#### 2.1 지역 점수 추출

각 지역의 점수는 다음 요소를 종합하여 계산됩니다:

| 항목 | 점수 기여도 | 계산 방법 |
|------|-----------|----------|
| **업종 다양성** | 최대 +20점 | 업종 수 × 1.5 (최대 20점) |
| **소비 안정성** | 최대 +10점 | 안정적(+10), 보통(+5) |
| **성장률** | -10 ~ +15점 | 상승(+15), 유지(+5), 하락(-10) |
| **기본 점수** | 50점 | 모든 지역 기본 |

#### 2.2 예시: 마포구

```python
기본 점수: 50점
+ 업종 다양성: 8개 × 1.5 = 12점
+ 소비 안정성: 안정적 = 10점
+ 성장률: +1.67% (상승) = 15점
= 총 87점 (정규화하여 82점)
```

---

### STEP 3: 사용자 선호도 점수

#### 3.1 업종 매칭 점수

```python
사용자 선호 업종: ['한식', '커피전문점', '영화/공연']
지역 특화 업종: '한식 특화 (90.5%)'

→ 매칭! +15점
```

#### 3.2 연령대별 가중치

| 연령대 | 선호하는 지역 특성 | 가중치 |
|--------|------------------|--------|
| 20-30대 | 성장하는 지역 | 성장률 +15점 |
| 40-60대 | 안정적인 지역 | 안정성 +10점 |

#### 3.3 다양성 선호도

```python
사용자 관심 업종 수 > 5개?
  YES → 다양성 높은 지역 선호 (+10점)
  NO  → 특화된 지역 선호 (+10점)
```

---

### 최종 점수 계산

```python
최종 점수 = (선호도 점수 × w1) + (지역 점수 × w2)

예시:
  선호도 점수: 65점
  지역 점수: 82점
  가중치: w1=0.6, w2=0.4
  
  최종 점수 = (65 × 0.6) + (82 × 0.4)
           = 39 + 32.8
           = 71.8점
```

---

## 🎨 사용 시나리오

### 시나리오 1: 20대 남성 직장인

```python
result = system.recommend_regions(
    age=28,
    gender='남',
    income_level='중',
    preferences={
        'preferred_industries': ['한식', '커피전문점', '술집'],
        'priorities': {'preference_match': 0.7, 'score': 0.3}
    },
    top_n=5
)
```

**추천 결과**:
```
1위. 마포구 (매칭 점수: 75.8점)
   - 선호도: 65.0점 | 지역 점수: 82.0점
   - 이유: 20대 남성에게 인기, 기타요식 관심사에 적합, 성장하는 지역

2위. 관악구 (매칭 점수: 73.4점)
   - 선호도: 60.0점 | 지역 점수: 76.0점
   - 이유: 일식 특화, 젊은 층이 많이 방문

3위. 영등포구 (매칭 점수: 71.6점)
   ...
```

### 시나리오 2: 40대 남성 가장

```python
result = system.recommend_regions(
    age=45,
    gender='남',
    income_level='중',
    preferences={
        'preferred_industries': ['대형마트', '한식', '주유소'],
        'priorities': {'preference_match': 0.5, 'score': 0.5}
    },
    top_n=5
)
```

**추천 결과**:
```
1위. 금천구 (매칭 점수: 79.3점)
   - 선호도: 92.6점 | 지역 점수: 66.0점
   - 이유: 대형마트 특화 (47.7%), 안정적 소비

2위. 강동구 (매칭 점수: 77.8점)
   - 선호도: 92.6점 | 지역 점수: 63.0점
   - 이유: 대형마트 특화 (87.1%), 가족 단위 방문 많음
```

### 시나리오 3: 30대 여성 프리랜서

```python
result = system.recommend_regions(
    age=35,
    gender='여',
    income_level='고',
    preferences={
        'preferred_industries': ['카페', '레스토랑', '문화시설'],
        'priorities': {'preference_match': 0.6, 'score': 0.4}
    },
    top_n=5
)
```

**추천 결과**:
```
1위. 강남구 (매칭 점수: 76.0점)
   - 선호도: 60.0점 | 지역 점수: 92.0점
   - 이유: 업종 다양성 높음 (14개), 고급 소비 지역

2위. 서초구 (매칭 점수: 74.2점)
   - 선호도: 65.0점 | 지역 점수: 83.5점
   - 이유: 한식 특화, 안정적 소비 패턴
```

---

## 📈 추천 품질 향상 팁

### 1. 가중치 조정

```python
# 사용자 선호를 더 중시하는 경우
priorities = {'preference_match': 0.8, 'score': 0.2}

# 지역 객관적 점수를 더 중시하는 경우
priorities = {'preference_match': 0.3, 'score': 0.7}

# 균형잡힌 추천
priorities = {'preference_match': 0.5, 'score': 0.5}
```

### 2. 상세한 선호사항 입력

```python
preferences = {
    'preferred_industries': [
        '한식',           # 기본 선호
        '커피전문점',     # 추가 선호
        '영화/공연'       # 여가 활동
    ],
    'time_preference': '저녁',  # 주요 활동 시간
    'budget': 50000,            # 평균 예산
    'priorities': {
        'preference_match': 0.6,
        'score': 0.4
    }
}
```

### 3. 추천 개수 조정

```python
# TOP 3 추천 (핵심만)
result = system.recommend_regions(..., top_n=3)

# TOP 10 추천 (더 많은 옵션)
result = system.recommend_regions(..., top_n=10)
```

---

## 🔧 커스터마이징

### 1. 새로운 점수 요소 추가

`step3_integrated_recommendation.py`의 `_extract_step2_score` 메서드를 수정:

```python
def _extract_step2_score(self, region_data: Dict) -> float:
    score = 50.0
    
    # 기존 점수 계산
    # ... (업종다양성, 소비안정성, 성장률)
    
    # 새로운 요소 추가: 접근성 점수
    if 'accessibility' in region_data:
        accessibility = region_data['accessibility']
        if accessibility == '높음':
            score += 10
    
    return min(100.0, max(0.0, score))
```

### 2. 세그먼트별 맞춤 로직

```python
def _calculate_preference_score(self, ...):
    score = 40.0
    
    age_group = user_profile['segment_info']['age_group']
    
    # 20대는 트렌디한 지역 선호
    if age_group == '20s':
        if '성장' in str(region_data.get('성장률', '')):
            score += 20
    
    # 40-50대는 실용적인 지역 선호
    elif age_group in ['40s', '50s']:
        if '대형마트' in str(region_data.get('특징', '')):
            score += 15
    
    return min(100.0, score)
```

---

## 📊 결과 분석

### 출력 JSON 구조

```json
{
  "user_info": {
    "age": 28,
    "gender": "남",
    "income_level": "중",
    "matched_segment": "20_29세 남",
    "avg_budget": 30000
  },
  "recommendations": [
    {
      "rank": 1,
      "region_name": "마포구",
      "match_score": 75.8,
      "preference_score": 65.0,
      "step2_score": 82.0,
      "reason": "20대 남성에게 인기, 기타요식 관심사에 적합, 성장하는 지역",
      "region_details": {
        "특징": "ZZ_나머지 특화 (52.7%)",
        "업종다양성": "낮음(8개)",
        "소비안정성": "안정적(17.1%)",
        "성장률": "+1.67% (↑상승)"
      }
    }
  ]
}
```

### 결과 저장

```python
# 자동 파일명 생성
system.save_recommendations(result)

# 커스텀 파일명
system.save_recommendations(result, 'my_recommendation.json')
```

---

## 🎯 성능 최적화

### 1. 프로필 캐싱

```python
# 프로필을 한 번만 로드하고 재사용
system = IntegratedRecommendationSystem(...)

# 여러 사용자에 대해 반복 사용
for user in users:
    result = system.recommend_regions(**user)
```

### 2. 배치 처리

```python
users = [
    {'age': 28, 'gender': '남', 'income_level': '중'},
    {'age': 35, 'gender': '여', 'income_level': '고'},
    # ... 더 많은 사용자
]

results = []
for user in users:
    result = system.recommend_regions(**user, top_n=5)
    results.append(result)
```

---

## ⚠️ 주의사항

1. **STEP 2 파일 필수**: 지역 점수 파일이 없으면 STEP 1 기반 추천만 제공됩니다.

2. **데이터 동기화**: STEP 1과 STEP 2 데이터의 기준 시점이 다르면 추천 품질이 저하될 수 있습니다.

3. **메모리 사용**: 대량의 사용자를 한 번에 처리할 때는 메모리 사용량을 모니터링하세요.

4. **가중치 범위**: `priorities`의 값은 0-1 사이여야 하며, 합이 1일 필요는 없습니다.

---

## 📞 트러블슈팅

### 문제 1: 프로필 파일을 찾을 수 없습니다

```
FileNotFoundError: 프로필 파일을 찾을 수 없습니다: outputs/step1_static_profiles.json
```

**해결**:
```bash
python3 step1_static_profile_generation.py
```

### 문제 2: STEP 2 파일 형식 오류

```
ValueError: 지원하지 않는 파일 형식입니다: .xlsx
```

**해결**: CSV 또는 JSON 형식으로 변환하세요.

```python
import pandas as pd
df = pd.read_excel('step2_scores.xlsx')
df.to_csv('step2_scores.csv', index=False)
```

### 문제 3: 모든 지역 점수가 동일

STEP 2 파일의 컬럼명을 확인하세요:
- `특징`, `업종다양성`, `소비안정성`, `성장률` 컬럼이 있어야 합니다.

---

## 📚 참고 자료

- [STEP 1 상세 가이드](./STEP1_가이드.md)
- [프로젝트 전체 정리](./프로젝트_전체정리.md)
- [PPT 발표 자료](./PPT_최종정리_프로세스_결과.md)

---

## 🎉 완료!

이제 사용자의 나이, 성별, 취향을 입력받아 서울시 25개 구 중 
가장 적합한 지역을 추천할 수 있습니다!

```python
# 간단한 예제
from step3_integrated_recommendation import IntegratedRecommendationSystem

system = IntegratedRecommendationSystem(
    step1_dir='outputs',
    step2_file='outputs/seoul_all_gu_final.csv'
)

result = system.recommend_regions(
    age=28, gender='남', income_level='중',
    preferences={'preferred_industries': ['한식', '카페']},
    top_n=5
)

system.print_recommendations(result)
```

**즐거운 추천 시스템 활용 되세요! 🚀**



